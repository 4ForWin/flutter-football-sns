import 'package:flutter/material.dart';

class IntroLevelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(children: [Text('당신의 풋살 실력은 어느 정도인가요?')]),
    );
  }
}
